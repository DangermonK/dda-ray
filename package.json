{
  "name": "@dangermonk/dda-ray",
  "version": "1.0.0",
  "description": "step based Digital Differential Analyzer (DDA) algorithm",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "patch": "npm version patch && npm publish --scope public",
    "minor": "npm version minor && npm publish --scope public",
    "major": "npm version major && npm publish --scope public",
  "author": "dangermonk",
  "license": "ISC",
  "dependencies": {
    "typescript": "^4.9.4"
  }
}
